#workout
	%p= @track.date
	%p= @track.track
	%p= @track.condition
	%p= @track.length

= link_to "Back", root_path
|
= link_to "Edit", edit_track_path(@track)
|
= link_to "Delete", track_path(@track), method: :delete, data: { confirm: "Are you sure?" }
